// function findMatching(array,string){
//     const results = array.filter(element => {
//        if( element[0].toLowerCase()+element.slice(1,element.length)===string ||element=== string ){
//             return console.log(string)
//         }
//     })
        
//     return results;
// }

function findMatching(array, string){
    const results = array.filter(element => {
        if (element.toLowerCase() === string)
        return results
    })
}
